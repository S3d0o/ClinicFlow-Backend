namespace Shared.ResultPattern
{
    public enum ErrorType
    {
        Failure,
        Validation,
        NotFound,
        Unauthorized,
        Forbidden,
        InvalidCredentials,
        Conflict
    }
}
